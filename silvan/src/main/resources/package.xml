<assembly
        xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2
        http://maven.apache.org/xsd/assembly-1.1.2.xsd">
    <id>package-SNAPSHOT</id>
    <formats>
        <format>tar.gz</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    <fileSets>
        <fileSet>
            <directory>${basedir}</directory>
            <outputDirectory>${rpm.install.soft.dir}</outputDirectory>
            <includes>
                <include>bin/**/*</include>
                <include>config/**/*</include>
                <include>db/**/*</include>
                <include>install/**/*</include>
            </includes>
        </fileSet>
    </fileSets>
    <dependencySets>
        <dependencySet>
            <outputFileNameMapping>${artifact.artifactId}-${artifact.baseVersion}.${artifact.extension}
            </outputFileNameMapping>
            <outputDirectory>${rpm.install.soft.dir}/lib</outputDirectory>
        </dependencySet>
    </dependencySets>
</assembly>